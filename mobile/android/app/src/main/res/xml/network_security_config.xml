<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
  <!--
    Production hardening:
    - Release builds set usesCleartextTraffic=false via manifest placeholder.
    - Debug builds can enable cleartext for local dev hosts.
  -->
  <base-config cleartextTrafficPermitted="false">
    <trust-anchors>
      <certificates src="system" />
    </trust-anchors>
  </base-config>
  <!-- Local dev hosts (emulator/device) -->
  <domain-config cleartextTrafficPermitted="true">
    <domain includeSubdomains="true">192.168.1.168</domain>
    <domain includeSubdomains="true">10.0.2.2</domain>
    <domain includeSubdomains="true">localhost</domain>
    <domain includeSubdomains="true">127.0.0.1</domain>
  </domain-config>
</network-security-config>
