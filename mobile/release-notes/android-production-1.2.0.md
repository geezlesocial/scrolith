Improved Android startup stability and loading responsiveness with the Phase 3
native shell foundation. Added a feature-flagged hybrid Notifications Center
and a compact native navigation pilot while preserving the WebView-owned
authentication, messaging, calling, notifications, and deep-link flows.

Native Phase 3 experiences remain disabled by default in release builds.
