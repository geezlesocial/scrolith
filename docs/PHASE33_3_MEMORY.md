# Phase 33.3 — Memory & Context

Reuses Phase 33.2 `AIUserMemory` plus copilot context fields:

- conversation context (Phase 33.1 conversations)  
- user preferences / topics  
- page path / entity context (request-scoped, not long-term profile)  
- skill / workspace hints  

User controls: view/edit/export/delete via `/settings/ai` and `/discovery` memory tab.
