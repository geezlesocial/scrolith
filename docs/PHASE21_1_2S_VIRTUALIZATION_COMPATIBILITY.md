# Phase 21.1.2S — Virtualization Compatibility

Progress updates no longer change outer player height/width. Row remeasure should not fire on playback ticks. Outer dimensions change only on first error reservation (once) or attachment identity change.
