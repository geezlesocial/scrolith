# Phase 33.3 — Monitoring

- Copilot latency / blocks / beta denials  
- Provider health includes NATIVE  
- Keep `SCROLITHA_AI_ENABLE_PROVIDER_CALLS=false` in production  
- Enable copilot for admins + allowlist first (`betaAllowlistOnly: true`)  
