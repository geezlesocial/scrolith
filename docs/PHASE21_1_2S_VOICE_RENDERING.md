# Phase 21.1.2S — Voice Rendering

Player: `VoiceNotePlayer` data-phase `21.1.2S`.

- Waveform: fixed bar count/heights from `getDeterministicWaveform(attachmentId|src)`
- Progress: transform-only fill (`data-testid=voice-progress-fill`)
- Timers: refs + tabular-nums fixed min width
- Speed: fixed `w-10 h-6` control
- Error: reserved band; does not collapse card
- contain: layout paint
