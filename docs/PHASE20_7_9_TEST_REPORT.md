# Phase 20.7.9 — Test Report

```
npx tsx --test src/services/scrolitha/__tests__/scrolitha.phase2079.capabilityActivation.spec.ts
```

Asset URL shape + non-E2EE + write confirmation phrasing.

## Production smoke after flags

- GET /api/scrolitha/public-profile → capability statuses
- Profile avatar URL matches official asset
- Messages participant avatar for Scrolitha uses official photo
