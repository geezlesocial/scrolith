# Phase 33.1 — Testing

## Backend

`scrolithaAi.assistant.spec.ts` — language/tokens, prompt library, conversations, feedback, chat/rewrite/composer/translate/draft/search/notification/prompt use.

`scrolithaAi.foundation.spec.ts` — Phase 33.0 regression.

## Frontend

`assistant.phase331.spec.ts` — UI structure, composer non-submit, service paths, `/assistant` route.

## Defaults verified

Flags off → blocked; consent off → blocked; MOCK path when provider calls disabled.
