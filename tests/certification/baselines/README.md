# Performance baselines (Phase 21.1.6)

Baselines are written by `performance.baseline.spec.ts` when authenticated cert runs.

Files:

- `{surface}-{viewport}-latest.json` — most recent sample
- dated copies for historical comparison

Do not commit secrets. Baseline JSON is metrics-only (safe to commit after a real run).
