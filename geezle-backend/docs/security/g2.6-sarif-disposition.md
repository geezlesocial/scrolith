# G2.6 SARIF disposition — PRE-REMEDIATION SARIF DISPOSITION

This document accompanies `g2.6-sarif-disposition.csv` and
`g2.6-sarif-disposition.json`. It is evidence for the exact pre-remediation
SARIF produced by Security run `35924078157` for parent source commit
`5c0fd8ba60371746b48f0d74485a15e2d413f790`.

Raw SARIF: `codeql-results-complete.raw.sarif` from the retained security
artifact. SHA-256:
`E936FD5845766F89B85CEE5E923E93078F17242C9DDB4DDC8BF1EA8586916A7B`.

The inventory contains exactly 120 rows, one for every result across every
SARIF run. Composite fingerprints retain all SARIF partial-fingerprint pairs
and add the rule, artifact path, and primary line so results that share a CodeQL
line hash remain distinct. Spreadsheet formula-leading values are neutralized
in the derived CSV.

## Disposition totals

| Category | Count |
| --- | ---: |
| confirmed vulnerability | 26 |
| accepted safe pattern | 78 |
| verified false positive | 2 |
| build/test-only finding | 14 |
| likely vulnerability | 0 |
| Total | 120 |

The 26 confirmed/remediated pre-remediation results are:

- `js/remote-property-injection`: 16 results. Prepared fix: shared safe-key
  filtering rejects `__proto__`, `prototype`, and `constructor` before dynamic
  assignment while preserving valid keys.
- `js/log-injection`: 9 results in `adminController.ts`. Prepared fix:
  bounded single-line `safeLogLine` serialization for request, user, and error
  context.
- `js/path-injection`: 1 result at the favicon copy path in
  `filesController.ts`. Prepared fix: canonical upload-root containment with
  `isPathWithin` before filesystem access.

The 78 accepted safe-pattern records retain concise evidence for the actual
guard, route boundary, input type, or non-sensitive use reviewed. The two
verified false positives are the no-op identity replacement and an internal
plan identifier generated by `Math.random()` that is not an authentication,
authorization, password, session, or capability value. The 14 build/test-only
records are confined to test fixtures under `src/__tests__/`.

The prior 111-row inventory was not reused as the authoritative result set.
Twelve prior rows are absent from the current SARIF (the removed historical
hardcoded-credential test/config rows and one prior log location); they are not
copied into the current inventory. The current CSV/JSON therefore has zero
stale rows and zero missing current results.

## Evidence-state boundary

This is explicitly a **PRE-REMEDIATION SARIF DISPOSITION**. The 26 findings
were present in the SARIF for `5c0fd8ba` and are addressed by the local source
changes prepared in this worktree; they have not disappeared from the current
SARIF merely because a local fix exists. A fresh post-remediation Security SARIF
must be generated and reconciled before any final G2 closure decision.

The local source changes and this evidence do not authorize deployment,
staging, ACR, Azure, DAST, or traffic changes.
