# G2.6 dependency disposition

Source checkpoint: `b69ca2941236c14c20a70e4e9b1c900b388c6d86`

The current Prisma 7 dependency graph was verified with `npm explain` and the
runtime image checks in security workflow `35514711326`.

| Advisory | Package and path | Installed | Fixed version | Reachability | Evidence | Decision |
| --- | --- | ---: | ---: | --- | --- | --- |
| GHSA-ggr8-5vv4-36mx | `prisma@7.10.0` → `@prisma/config@7.10.0` → `deepmerge-ts@7.1.5` | 7.1.5 | `>=8.0.0` | Prisma CLI/config migration and build tooling; absent from final runtime image | `npm explain`; CI Docker runtime-content check passed; final-image SBOM generated | `build/test-only finding`; Prisma parent upgrade or owner approval required |
| GHSA-3f6p-5ww8-9rcr | `prisma@7.10.0` → `mysql2@3.15.3` | 3.15.3 | `>3.22.0` | Prisma CLI/config tooling; absent from final runtime image | `npm explain`; CI Docker runtime-content check passed; final-image SBOM generated | `build/test-only finding`; Prisma parent upgrade or owner approval required |

`mysql2` is not a Scrolith application runtime dependency; the application uses
the PostgreSQL adapter. No direct dependency or blanket override was added.

The audit tool proposes a Prisma 6 downgrade for these paths. That is not an
approved remediation because it would reverse the validated Prisma 7 adapter
migration. A compatible Prisma 7 parent release that removes both paths must be
validated before changing the dependency set.

There is currently no owner-approved exception. G2 remains blocked until either:

1. a compatible Prisma 7 release removes the vulnerable paths and all Prisma,
   migration, integration, build, and image checks pass; or
2. the security/dependency owner approves a time-bound exception with an owner,
   compensating controls, remediation deadline, expiry date, and retest plan.
