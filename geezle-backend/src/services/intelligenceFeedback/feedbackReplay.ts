export const feedbackReplay = {
  enabled: false,
  describe() {
    return {
      enabled: false,
      reason: 'Phase 19.2 prepares replay boundaries only; replay execution is deferred.'
    };
  }
};
